services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/sonique.db:/app/backend/sonique.db
      - ./backend/downloads:/app/backend/downloads
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
