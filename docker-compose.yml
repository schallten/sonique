services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/database:/app/backend/database
      - ./backend/downloads:/app/backend/downloads
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
